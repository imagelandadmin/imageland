// This file defines interface for plain old json objects
// that cross the boudary to external services (where typings
// don't already exist or can't be generated).

export interface User {
    //TODO inspect json to see what fields are returned by cognito
}